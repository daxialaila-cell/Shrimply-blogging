<% if (theme.twikoo && theme.twikoo.envId) { %>
  <div id="twikoo"></div>
  <script type="text/javascript">
    Shrimply.utils.loadComments('#comments', function() {
      Shrimply.utils.createScript('<%= url_join(theme.static_prefix.twikoo, 'twikoo.all.min.js') %>', function() {
        var options = Object.assign(
          <%- JSON.stringify(theme.twikoo || {}) %>,
          {
            el: '#twikoo',
            path: '<%= theme.twikoo.path %>',
            onCommentLoaded: function() {
              Shrimply.utils.listenDOMLoaded(function() {
                var imgSelector = '#twikoo .tk-content img:not(.tk-owo-emotion)';
                Shrimply.plugins.imageCaption(imgSelector);
                Shrimply.plugins.fancyBox(imgSelector);
              });
            }
          }
        )
        twikoo.init(options)
      });
    });
  </script>
  <noscript>Please enable JavaScript to view the comments</noscript>
<% } %>
