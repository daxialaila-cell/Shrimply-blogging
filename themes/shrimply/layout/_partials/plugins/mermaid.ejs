<script>
  Shrimply.utils.createScript('<%= url_join(theme.static_prefix.mermaid, 'mermaid.min.js') %>', function() {
    mermaid.initialize(<%- JSON.stringify(theme.post.mermaid.options || {}) %>);

    Shrimply.utils.listenDOMLoaded(function() {
      Shrimply.events.registerRefreshCallback(function() {
        if ('mermaid' in window) {
          mermaid.init();
        }
      });
    });
  });
</script>
