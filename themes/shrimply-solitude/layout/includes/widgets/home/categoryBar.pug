.category-bar-items#category-bar-items(class=is_home() ? 'home' : '')
    each item in site.categories.find({ parent: { $exists: false } }).data
        .category-bar-item(id=item.name)
            a(href=url_for(item.path))= item.name
