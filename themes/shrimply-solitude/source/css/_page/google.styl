.google-vignette-test
    animation 0.6s ease 0s 1 normal none running to_show

    > body
        background-color var(--sb-maskbgdeep)

    .header-panel
        background-color transparent
        border none

        .header-message
            color var(--sb-fontcolor)

        .close-button
            line-height inherit
            background var(--sb-maskbg)
            border-radius 50%
            padding 4px
            margin 12px
            display flex
            align-items center
            justify-content center

    .container
        ins
            iframe
                border-radius 12px
                border var(--style-border)
                box-shadow var(--sb-shadow-border)

                > body
                    background-color var(--sb-maskbg)